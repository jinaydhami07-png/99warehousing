# Not used.

Property photos and floor plans go to AWS S3 (see `server/.env` and the "Photos and floor
plans" section of the deploy README). Nothing is written to this folder.

It is kept only so the path exists on hosts that expect it; if you find image files here,
they predate the move to S3.
